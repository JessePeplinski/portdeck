#!/bin/sh
set -eu
provider_root="$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd)"
exec "$provider_root/../PortDeckRuntime/bin/node" \
  "$provider_root/node/node_modules/netlify-cli/bin/run.js" "$@"
